- title "Game #{@game.code}"

%p
  %strong Championship:
  = link_to @game.championship.name, championship_path(@game.championship)

%p
  %b Referis:
  - @game.referis.each do |r|
    %p
      == Partidos: #{r.games.size}
      %br
      == Nombre: #{full_name(r)}
.clear
%div{:class => "left", :style =>"width: 45%;" }
  %p
    %span= @local.photo.thumb.exists? ? image_tag(@local.photo.thumb.url(:thumb)) : image_tag('missing.png')
    %h3= @local.name
  - @local.players.each do |p|
    %p
      %span= image_tag(p.gravatar_url(:size => 20))
      %span= link_to full_name(p), player_path(p)
      %span= p.can_play? ? image_tag('check.png') : image_tag('error.png')
%div{:class => "right", :style =>"width: 45%;" }
  %p
    %span= @visitor.photo.thumb.exists? ? image_tag(@visitor.photo.thumb.url(:thumb)) : image_tag('missing.png')
    %h3= @visitor.name
  - @visitor.players.each do |p|
    %p
      %span= image_tag(p.gravatar_url(:size => 20))
      %span= link_to full_name(p), player_path(p)
      %span= p.can_play? ? image_tag('check.png') : image_tag('error.png')
.clear
            
